name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Configure git
        # The tests build throwaway repositories, which need an identity.
        run: |
          git config --global user.email "ci@example.com"
          git config --global user.name "CI"
      # Pinned: an unpinned linter or test runner turns an upstream release
      # into a red build on a commit that changed nothing.
      - run: pip install pytest==8.4.2
      - run: python -m pytest tests -q

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install ruff==0.11.11
      - run: ruff check .
      - run: ruff format --check .
